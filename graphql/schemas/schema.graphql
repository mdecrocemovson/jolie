query GetPolitician($id: String!) {
  fetchPolitician(id: $id) {
    id
    firstName
    lastName
    party {
      name
    }
    image
    description
    issues {
      description
      name
    }
  }
}
